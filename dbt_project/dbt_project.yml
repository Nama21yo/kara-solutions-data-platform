name: "medical_data_platform"
version: "1.0.0"
config-version: 2

profile: "default"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  medical_data_platform:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
